

export interface User {
    id: number;
    name: string;
    contactNumber: string;
    email: string;
    role: string;
 // Include password if it's safe and necessary, generally not recommended
  }
  